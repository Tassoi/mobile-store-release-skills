name: release_risk_fixture
description: "An anonymized Flutter release-readiness fixture."
publish_to: "none"
version: 2.4.0+42

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
